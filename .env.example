FLASK_ENV=development
SECRET_KEY=change-me

# Supabase (server-side recommended)
SUPABASE_URL=
# Use ONE of these keys (service role is for server-only; never expose it to browser)
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=

